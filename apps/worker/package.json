{
  "name": "@domain-manager/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json --pretty false",
    "start": "node dist/main.js",
    "lint": "echo 'worker lint configured in next milestone'",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false",
    "test": "node --test"
  },
  "dependencies": {
    "bullmq": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.9.0"
  }
}
